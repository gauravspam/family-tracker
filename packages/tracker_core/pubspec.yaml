name: tracker_core
description: Shared models and client abstractions for Family Tracker
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  dio: ^5.5.0
  meta: ^1.12.0
  web_socket_channel: ^3.0.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
