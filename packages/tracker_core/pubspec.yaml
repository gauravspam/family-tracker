name: tracker_core
description: Shared models and client abstractions for Family Tracker
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  dio: ^5.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
